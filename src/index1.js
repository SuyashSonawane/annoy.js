class Annoy {
    _dataset = []
    _itemMap = null
    constructor(elements) {
        // TODO add checks
        this._dataset = elements
        this._itemMap = new Map();
        this._dataset.forEach(element => {
            // this._itemMap.
        });
    }
}

module.exports = Annoy