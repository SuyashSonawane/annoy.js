export type DataPoint = {
    el: Element,
    x: number,
    y: number,
    id: string,
    className: string,
    active: boolean
}

export type FunctionDataPoint = {
    el: HTMLElement,
    elX: number,
    elY: number,
    mouseX: number,
    mouseY: number

}